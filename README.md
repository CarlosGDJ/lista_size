# Lista Size

Este programa lista los archivos del directorio actual y escribe sus nombres y tamaños en el archivo `/tmp/lista_sz`.

## Uso
Compila y ejecuta el programa:
```bash
gcc -o lista_size lista_size.c
./lista_size
